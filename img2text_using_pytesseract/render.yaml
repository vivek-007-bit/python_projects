services:
  - type: web
    name: flask-ocr
    env: python
    buildCommand: |
      pip install -r requirements.txt
      apt-get update
      apt-get install -y tesseract-ocr
    startCommand: gunicorn wsgi:app
